
## MOF backtest recon (07:54 UTC / 02:54... 05:54 CT) — data findings
- CORRECTION to Chris premise: MOF NQ = 15 trades (14 NQ + 1 MNQ), NOT 49. 49 = whole cross-instrument MOF (M2K 24, MCL 13, NQ 14, MNQ 1). Only 1 real Stop-loss exit in entire 52-row MOF history -> cannot measure observed-stop recovery; must SIMULATE SL grid on real paths.
- DATA: trades DB = Postgres orbfutures_dashboard @127.0.0.1:5432, table `trades` (DSN in knowledge/trading-data-catalog/02-trades-db.md, NOT in .env). Real NQ 1-min bars in ~/repos/orbfutures/data/ (nqfut_NQM6/NQU6 parquet + NQ_06-26 txt) cover 03-10->07-19 = only 5 of 15 MOF trades. 9 recent (07-21->08-13) MISSING; no fetch script; Polygon plan = ETF-proxy only (no CME futures); Alpaca no futures.
- HARNESS READY: ~/repos/orbfutures/backtest/ has load_nq_futures() (h154_flip_short_only_gated.py:95), h173_maxhold_comparison.py (= hold-to-close question), h170/h171 trailing stops, mov_engine.py SL/TP sim. Adapt, feed 15 DB trade rows as entries.
- ASKED CHRIS: export NQ 1-min bars 2026-04-20 -> 08-13 (RTH 8:30-15:00 CT) from terminal = ground truth, closes gap. QQQ 1-min proxy is the fallback (real bars all dates, repo already uses QQQ as MNQ proxy) if he prefers not to export.
- NEXT: on export/proxy decision -> build sim (SL-grid trigger% + recover-by-close% + trailing variants), loop analyst as owner, show n behind every number. Sample n=15 = directional not tight, flagged.
